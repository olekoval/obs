```python

from typing import Optional, List, Dict, Any


def function_name(param_1: str, param_2: int, optional_param: Optional[float] = None) -> Dict[str, 
    """Короткий опис того, що робить функція (один рядок).

    Детальніший опис логіки роботи функції, якщо це необхідно для розуміння 
    складних алгоритмів чи бізнес-правил.

    Args:
        param_1: Опис першого параметра із зазначенням очікуваного формату.
        param_2: Опис другого параметра.
        optional_param: Опис необов'язкового параметра (за замовчуванням None).

    Returns:
        Словник із результатами виконання або ключовими даними.

    Raises:
        ValueError: Опис ситуації, за якої викидається виняток.
    """
    # Тіло функції, де Copilot буде пропонувати ідеальні продовження
    result = {}
    return result
