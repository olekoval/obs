```marmaid
graph TD
    %% Стилі для блоків
    classDef flask fill:#ffd1d1,stroke:#ff6b6b,stroke-width:2px;
    classDef dash fill:#d1e8ff,stroke:#54a0ff,stroke-width:2px;
    classDef common fill:#e2ffd1,stroke:#10ac84,stroke-width:2px;

    %% Архітектура
    subgraph Imports ["1. Імпорти"]
        A["from flask import Flask, render_template_string<br>from dash import Dash, html, callback, Input, Output"]
    end

    subgraph Initialization ["2. Ініціалізація додатків"]
        B["server = Flask(__name__)"]:::flask
        C["app = Dash(__name__, server=server, routes_pathname_prefix='/dash/')"]:::dash
    end

    subgraph Layout ["3. Інтерфейс Dash"]
        D["app.layout = html.Div([ ... ])"]:::dash
    end

    subgraph Logic ["4 & 5. Обробка запитів веб-сервером"]
        E["@server.route('/')<br>def index():<br>(Звичайні сторінки Flask)"]:::flask
        F["@callback(Output(...), Input(...))<br>def update(...):<br>(Інтерактивні колбеки Dash)"]:::dash
    end

    subgraph Execution ["6. Запуск"]
        G["if __name__ == '__main__':<br>    server.run(port=5000)"]:::common
    end

    %% Зв'язки
    A --> B
    B --> C
    C --> D
    B --> E
    D --> F
    E --> G
    F --> G
```