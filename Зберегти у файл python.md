**Магічна команда `%%writefile` у jupyter lab:** 

Якщо вам потрібно зберегти лише вміст <span style='color: yellow'>однієї конкретної клітинки</span>, додайте на її початку рядок із назвою майбутнього файлу:

```python
%%writefile name_script.py
```
